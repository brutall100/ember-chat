# Copy this file to .env and fill in your own values.
# If DB_HOST is empty, the server keeps messages in memory instead of MySQL.

PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=ember_chat
DB_PASSWORD=change-me
DB_DATABASE=ember_chat
